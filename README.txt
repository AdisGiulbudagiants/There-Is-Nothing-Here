В этом небольшом проекте я хотел показать, умение пользоваться библиотекой GreenSock(gsap),
умение адаптировать сайт под разные разрешения мониторов,а также показать навык работы с функциональными компонентами в React

Cтэк:
    React (TypeScript)
    Tailwindcss
    SASS
    Git

Да, стэк не велик, но тут по сути и проект не большой)) Нет смысла его перегружать библиотеками и прочим.


################################################################################################################################
################################################################################################################################
################################################################################################################################


In this small project, I wanted to show the ability to use the GreenSock (gsap) library,
the ability to adapt the site to different monitor resolutions, as well as show the skill of working with functional components in React

Stack:
    React (TypeScript)
    Tailwindcss
    SASS
    Git

The stack is not large, but here, in fact, the project is not large)) There is no point in overloading it with libraries and other things.
